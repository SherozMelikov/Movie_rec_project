📁 app/api/README.md
This folder contains all FastAPI route definitions (API endpoints).
Files here should only define routes and call services.
No model loading, no business logic, no data processing should be done here.
Used by: React frontend.